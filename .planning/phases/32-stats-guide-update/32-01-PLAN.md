---
phase: 32-stats-guide-update
type: execute
---

<objective>
Document labor CPC methods in the stats guide with examples, field tables, and performance tips.

Purpose: Users need documentation for the two new stats methods (total_labor_cost, cost_per_car) and the updated report() that now includes labor/CPC data. Without docs, these features are invisible.
Output: Updated stats guide, API reference models page, and README — deployed to GitHub Pages.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-report-integration/31-01-SUMMARY.md
@.planning/phases/26-stats-docs-testing/26-01-SUMMARY.md

# Source files (current implementation to document):
@src/sonnys_data_client/resources/_stats.py
@src/sonnys_data_client/types/_stats.py

# Existing docs to update:
@docs/guides/stats.md
@docs/api/models.md
@README.md

**Constraining decisions:**
- Phase 26: Stats guide format established — method sections with code example + field table, "Choosing the Right Method" comparison table, performance tips as MkDocs admonitions
- Phase 31: report() now returns labor (LaborCostResult) and cost_per_car (CostPerCarResult) fields, using shared clock entry fetching
- Phase 31: Labor cost computed inline in report() (not delegating to total_labor_cost()) for shared data fetching efficiency
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update stats guide, API models page, and README with labor CPC documentation</name>
  <files>docs/guides/stats.md, docs/api/models.md, README.md</files>
  <action>
Update docs/guides/stats.md following the established format from Phase 26:

1. **"Choosing the Right Method" table** (line ~44): Add two new rows:
   - `total_labor_cost(start, end)` | `LaborCostResult` | Labor cost breakdown (regular/overtime) from clock entries | N_emp × ceil(days/14) + 1
   - `cost_per_car(start, end)` | `CostPerCarResult` | Labor cost per car washed | N_emp × ceil(days/14) + 4
   Update `report()` row: API Calls from "3 + ~N" to "4 + N_emp × ceil(days/14) + ~N" to reflect clock entry fetching.

2. **Add `total_labor_cost()` method section** after the `conversion_rate()` section (~line 196), following the established pattern (method signature heading, description paragraph, code example, field table):
   - Signature: `total_labor_cost(start, end) -> LaborCostResult`
   - Description: Fetches clock entries for all employees via 14-day windowed bulk fetch, aggregates regular/overtime costs. Each entry's cost = rate × hours. Site-code filtering applied locally.
   - Code example showing usage with formatted output for total_cost, regular_cost/hours, overtime_cost/hours
   - `LaborCostResult` field table: total_cost (float), regular_cost (float), overtime_cost (float), regular_hours (float), overtime_hours (float), total_hours (float), entry_count (int)

3. **Add `cost_per_car()` method section** after `total_labor_cost()`:
   - Signature: `cost_per_car(start, end) -> CostPerCarResult`
   - Description: Divides total labor cost by total washes. Value of 4.25 means $4.25 labor per car. Zero-division safe (returns 0.0 when no washes).
   - Code example
   - `CostPerCarResult` field table: cost_per_car (float), total_labor_cost (float), total_washes (int)

4. **Update `report()` section** (~line 197):
   - Update description to mention labor and cost_per_car KPIs
   - Update API calls description: "4 bulk API calls" (3 transaction + 1 employee list) plus "N_emp × ceil(days/14) clock-entry calls" plus "~N detail calls"
   - Add labor/CPC lines to the code example:
     ```python
     print(f"Labor cost: ${rpt.labor.total_cost:.2f}")
     print(f"Cost per car: ${rpt.cost_per_car.cost_per_car:.2f}")
     ```
   - Add `labor` and `cost_per_car` rows to StatsReport field table

5. **Update "Monthly performance snapshot" example** (~line 230) to include labor lines:
   ```python
   print(f"Labor:         ${rpt.labor.total_cost:>10,.2f}")
   print(f"  Regular:     ${rpt.labor.regular_cost:>10,.2f}")
   print(f"  Overtime:    ${rpt.labor.overtime_cost:>10,.2f}")
   print(f"Cost/car:      ${rpt.cost_per_car.cost_per_car:>10.2f}")
   ```

6. **Add labor performance tip** to Performance Tips section:
   Add a `!!! warning` admonition about clock entry API cost: fetching clock entries requires 1 employee list call + N_employees × ceil(days/14) clock entry calls. For 20 employees over 31 days, that's 1 + 20 × 3 = 61 API calls. Use `report()` when you need labor + other KPIs to share the fetch. For labor-only queries, `total_labor_cost()` is sufficient.

7. **Update docs/api/models.md**: Add LaborCostResult and CostPerCarResult mkdocstrings directives in the Stats section (after ConversionResult, before StatsReport), following the established pattern:
   ```
   ::: sonnys_data_client.types.LaborCostResult
       options:
         show_source: false
         heading_level: 3

   ::: sonnys_data_client.types.CostPerCarResult
       options:
         show_source: false
         heading_level: 3
   ```

8. **Update README.md**:
   - Line ~120: Add `total_labor_cost(start, end)`, `cost_per_car(start, end)` to the stats method list in the resource table
   - Line ~304: Add these methods to the "Methods:" line
   - Add labor example lines to the code block (~line 322):
     ```python
     labor = client.stats.total_labor_cost("2026-01-01", "2026-01-31")
     print(f"Labor: ${labor.total_cost:.2f}, Cost/car: ...")

     cpc = client.stats.cost_per_car("2026-01-01", "2026-01-31")
     print(f"Cost per car: ${cpc.cost_per_car:.2f}")
     ```
   - Update report() example to show labor/cpc fields
   - Update "Returns:" paragraph to mention LaborCostResult, CostPerCarResult, and updated StatsReport fields
  </action>
  <verify>
    - python -m mkdocs build --strict (no warnings or errors)
    - Visually confirm new method sections appear in built docs
  </verify>
  <done>
    - stats.md has total_labor_cost() and cost_per_car() sections with code examples and field tables
    - "Choosing the Right Method" table has 8 methods
    - report() section documents labor and cost_per_car fields
    - Performance tips include labor API cost warning
    - models.md has LaborCostResult and CostPerCarResult directives
    - README.md lists all 8 stats methods with labor/CPC examples
    - mkdocs build --strict passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy updated docs to GitHub Pages</name>
  <files>docs/</files>
  <action>Run `python -m mkdocs gh-deploy --force` to publish the updated documentation to GitHub Pages. This is required per PROJECT.md: "After any docs/ changes, you MUST run python -m mkdocs gh-deploy --force to publish updates to the live site."</action>
  <verify>Command completes without errors, outputs deployment URL</verify>
  <done>Updated docs live at https://christopher-nance.github.io/Sonnys-Data-API-V2/ with labor CPC documentation visible</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `python -m mkdocs build --strict` passes
- [ ] stats.md documents all 8 stats methods
- [ ] LaborCostResult and CostPerCarResult field tables present
- [ ] report() section includes labor and cost_per_car
- [ ] Performance tips cover clock entry API cost
- [ ] models.md has all 6 stats model directives
- [ ] README.md updated with labor/CPC methods and examples
- [ ] Docs deployed to GitHub Pages
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No mkdocs build warnings
- Documentation covers total_labor_cost(), cost_per_car(), and updated report()
- Phase 32 complete
</success_criteria>

<output>
After completion, create `.planning/phases/32-stats-guide-update/32-01-SUMMARY.md`
</output>
