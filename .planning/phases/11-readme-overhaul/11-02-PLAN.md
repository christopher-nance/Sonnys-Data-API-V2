---
phase: 11-readme-overhaul
plan: 02
type: execute
---

<objective>
Complete per-resource documentation with parameter tables, return types, and realistic code examples for all 8 resources.

Purpose: Make every resource method self-documenting in the README so developers never need to read source code to use the SDK.
Output: README.md with comprehensive resource documentation covering all methods, parameters, and return types.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-readme-overhaul/11-01-SUMMARY.md
@README.md
@src/sonnys_data_client/resources/_customers.py
@src/sonnys_data_client/resources/_items.py
@src/sonnys_data_client/resources/_employees.py
@src/sonnys_data_client/resources/_sites.py
@src/sonnys_data_client/resources/_giftcards.py
@src/sonnys_data_client/resources/_washbooks.py
@src/sonnys_data_client/resources/_recurring.py
@src/sonnys_data_client/resources/_transactions.py
@src/sonnys_data_client/_resources.py
@src/sonnys_data_client/types/_customers.py
@src/sonnys_data_client/types/_transactions.py
@src/sonnys_data_client/types/_recurring.py
@src/sonnys_data_client/types/_employees.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document standard resources (Customers, Items, Employees, Sites)</name>
  <files>README.md</files>
  <action>
Replace the current "## Resources" section's simple table with detailed per-resource subsections. Keep the summary table at the top as a quick reference, then add subsections below it.

For each of Customers, Items, Employees, Sites — create a `### resource_name` subsection containing:

1. **Methods list** with signatures showing actual parameter names (not just `**params`):
   - `list()` — note common query params: `startDate`, `endDate`, `site`, `region`, `limit`, `offset`
   - `get(id)` — where applicable
   - Special methods (e.g., `employees.get_clock_entries(employee_id, *, start_date, end_date)`)

2. **Code example** — realistic usage showing a common operation:
   - Customers: list with date filter, access model fields
   - Items: list all items
   - Employees: get clock entries for a date range
   - Sites: list all sites

3. **Return type note** — what Pydantic model is returned (e.g., `list[CustomerListItem]` for list, `Customer` for get)

Keep each resource subsection compact (10-20 lines). Use consistent formatting across all four. Reference the actual field names from the Pydantic models in examples (e.g., `customer.first_name`, `employee.employee_id`).

All list methods auto-paginate — mention this once at the top of the Resources section, not per-resource.
  </action>
  <verify>Confirm 4 resource subsections exist under Resources, each with methods, example, and return types. Verify field names match actual Pydantic model attributes.</verify>
  <done>Customers, Items, Employees, Sites each have dedicated subsections with methods, examples, and return types</done>
</task>

<task type="auto">
  <name>Task 2: Document account and transaction resources (Giftcards, Washbooks, Recurring, Transactions)</name>
  <files>README.md</files>
  <action>
Continue the per-resource subsections for the remaining 4 resources:

**Giftcards** (`### Giftcards`):
- Methods: `list(**params)` — list only, no get
- Example: list giftcards, access model fields
- Return type: `list[GiftcardListItem]`

**Washbooks** (`### Washbooks`):
- Methods: `list(**params)`, `get(id)`
- Example: list washbooks, get detail by ID
- Return types: `list[WashbookListItem]`, `Washbook`

**Recurring Accounts** (`### Recurring Accounts`):
- Methods: `list(**params)`, `get(id)`, `list_status_changes(**params)`, `list_modifications(**params)`, `list_details(**params)`
- This resource has the most methods — document each briefly
- Example: list accounts, get status changes for a date range
- Return types: `RecurringListItem`, `Recurring`, `RecurringStatusChange`, `RecurringModification`

**Transactions** (`### Transactions`):
- Methods: `list(**params)`, `get(id)`, `list_by_type(item_type, **params)`, `list_v2(**params)`, `load_job(*, poll_interval, timeout, **params)`
- Document `list_by_type` valid types: wash, prepaid-wash, recurring, washbook, giftcard, merchandise, house-account
- Document `list_v2` enriched fields: customer_id, is_recurring_plan_sale, is_recurring_plan_redemption, transaction_status
- Document `load_job` params: poll_interval (default 2.0), timeout (default 300.0), 24-hour date range limit, 20-min cache
- Example: list by type, load_job usage
- Return types: `TransactionListItem`, `Transaction`, `TransactionV2ListItem`, `TransactionJobItem`

Move the existing "### Batch Jobs" section content INTO the Transactions subsection (since load_job is a Transactions method). Remove the standalone Batch Jobs section to avoid duplication.

Same compact format as Task 1. Update the table of contents if Batch Jobs anchor changed.
  </action>
  <verify>Confirm 4 additional resource subsections exist. Verify Batch Jobs content is merged into Transactions. Verify ToC links still work. Check that field names and method signatures match actual source code.</verify>
  <done>All 8 resources fully documented with methods, examples, and return types. Batch Jobs merged into Transactions. ToC updated.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 8 resources have dedicated subsections
- [ ] Each subsection has: methods, example code, return types
- [ ] Method signatures match actual source code
- [ ] Batch Jobs content merged into Transactions (no standalone section)
- [ ] Table of contents links updated and valid
- [ ] README renders cleanly as markdown
- [ ] No content regression (error handling, logging, multi-site, requirements sections intact)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Every public method on every resource is documented in README
- Phase 11 complete — README overhaul delivered
</success_criteria>

<output>
After completion, create `.planning/phases/11-readme-overhaul/11-02-SUMMARY.md`
</output>
