---
phase: 11-readme-overhaul
plan: 01
type: execute
---

<objective>
Add professional README structure — badges, table of contents, and expanded SonnysClient constructor documentation.

Purpose: Transform the README from a functional reference into a polished, navigable SDK document that any developer or AI agent can scan quickly.
Output: README.md with badges, linked ToC, and complete client initialization documentation.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@README.md
@src/sonnys_data_client/__init__.py
@src/sonnys_data_client/_client.py
@src/sonnys_data_client/_version.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add badges, table of contents, and enhanced description</name>
  <files>README.md</files>
  <action>
Replace the current header section with:

1. **Title line** with short tagline: `# sonnys-data-client` followed by a one-line description.

2. **Badges row** using shields.io (static badges since this is a private GitHub package):
   - Python version: `![Python 3.10+](https://img.shields.io/badge/python-3.10%2B-blue)`
   - Pydantic v2: `![Pydantic v2](https://img.shields.io/badge/pydantic-v2-green)`
   - License MIT: `![License: MIT](https://img.shields.io/badge/license-MIT-yellow)`
   - Version: `![Version 0.1.0](https://img.shields.io/badge/version-0.1.0-brightgreen)`

3. **Enhanced description paragraph** — expand the current one-liner into 2-3 sentences covering: what it wraps, the resource-based interface pattern, auto-pagination, rate limiting, and Pydantic model returns. Keep it concise but informative.

4. **Table of contents** — markdown links to all major sections:
   - Installation
   - Quick Start
   - Client Configuration
   - Resources (with sub-links for each resource)
   - Batch Jobs
   - Error Handling
   - Logging
   - Multi-Site Usage
   - Requirements

Do NOT remove or modify any existing content below the current "## Installation" heading — only replace/expand the header area above it.
  </action>
  <verify>Open README.md and confirm: badges render as markdown images, ToC links are valid anchor references matching section headings, description is expanded</verify>
  <done>README has badges row, linked table of contents, and expanded project description above the Installation section</done>
</task>

<task type="auto">
  <name>Task 2: Add Client Configuration section with complete constructor documentation</name>
  <files>README.md</files>
  <action>
Insert a new "## Client Configuration" section between "## Quick Start" and "## Resources". Document:

1. **Constructor signature** in a code block:
   ```python
   SonnysClient(
       api_id: str,
       api_key: str,
       site_code: str | None = None,
       *,
       max_retries: int = 3,
   )
   ```

2. **Parameter table:**
   | Parameter | Type | Default | Description |
   with entries for api_id, api_key, site_code, max_retries — include that max_retries controls 429 retry attempts with exponential backoff.

3. **Context manager pattern** — show the `with` statement pattern (already in Quick Start but explain WHY — session cleanup). Also show the explicit `.close()` alternative.

4. **Authentication** — brief note that credentials are sent as `X-Sonnys-API-ID`, `X-Sonnys-API-Key`, and optional `X-Sonnys-Site-Code` headers on every request.

Keep it concise — this is reference documentation, not a tutorial. Use the same markdown style as the existing README sections.
  </action>
  <verify>Confirm the new section appears between Quick Start and Resources, parameter table renders correctly, code blocks have proper syntax highlighting markers</verify>
  <done>Client Configuration section present with constructor signature, parameter table, context manager docs, and auth note</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Badges render as valid markdown image syntax
- [ ] Table of contents links match actual section headings (anchor format)
- [ ] Client Configuration section has constructor signature, param table, context manager, auth note
- [ ] No existing content removed or broken
- [ ] README still renders cleanly as markdown
</verification>

<success_criteria>

- Both tasks completed
- All verification checks pass
- README has professional structure (badges, ToC, client config)
- No content regression from current README
</success_criteria>

<output>
After completion, create `.planning/phases/11-readme-overhaul/11-01-SUMMARY.md`
</output>
