---
phase: 12-api-reference-setup
plan: 01
type: execute
---

<objective>
Set up MkDocs Material with mkdocstrings for auto-generated Python API reference docs.

Purpose: Establish the documentation tooling foundation so all public API surfaces are rendered from docstrings automatically.
Output: Working mkdocs configuration with material theme, mkdocstrings plugin, and a landing page that builds successfully.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-readme-overhaul/11-02-SUMMARY.md

# Key files:
@pyproject.toml
@src/sonnys_data_client/__init__.py
@src/sonnys_data_client/_client.py

**Tech stack available:** hatchling build, pydantic v2, requests, pytest, ruff
**Established patterns:** src layout (src/sonnys_data_client/), optional-dependencies groups in pyproject.toml
**Constraining decisions:**
- [Phase 10]: pip-installable from GitHub, hatchling build system
- [Phase 11]: README has full resource documentation with method signatures and examples
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add docs dependencies to pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>Add a `docs` optional-dependencies group to pyproject.toml alongside the existing `dev` group:

```
docs = [
    "mkdocs-material>=9.5",
    "mkdocstrings[python]>=0.24",
]
```

Then install with `pip install -e ".[docs]"` to make mkdocs available.

Do NOT add mkdocs to the core `dependencies` list — docs tooling is development-only. Do NOT modify the existing `dev` group.</action>
  <verify>Run `pip install -e ".[docs]"` succeeds and `python -m mkdocs --version` outputs a version string</verify>
  <done>pyproject.toml has docs optional-dependencies group, mkdocs and mkdocstrings installed and importable</done>
</task>

<task type="auto">
  <name>Task 2: Create mkdocs.yml configuration</name>
  <files>mkdocs.yml</files>
  <action>Create mkdocs.yml at project root with:

- site_name: "Sonny's Data Client"
- site_description: "Python client for the Sonny's Carwash Controls Data API"
- repo_url pointing to the GitHub repo (use placeholder if unknown)
- theme: material with palette (slate/dark scheme for code-heavy docs), navigation features (tabs, sections, expand)
- plugins: search, mkdocstrings with Python handler configured for src layout:
  - paths: [src]
  - options: show_source: false, show_root_heading: true, show_root_full_path: false, members_order: source, show_bases: true, show_signature_annotations: true, separate_signature: true, docstring_style: google, merge_init_into_class: true
- markdown_extensions: toc (permalink true), admonition, pymdownx.highlight, pymdownx.superfences, pymdownx.snippets, attr_list
- nav structure:
  - Home: index.md
  - API Reference:
    - Client: api/client.md
    - Resources: api/resources.md
    - Models: api/models.md
    - Exceptions: api/exceptions.md

The `paths: [src]` is CRITICAL because the package uses src layout — without it mkdocstrings cannot find the modules. The `merge_init_into_class: true` ensures __init__ params show on the class itself (important for SonnysClient constructor docs).</action>
  <verify>Run `python -m mkdocs build --strict` — should fail only because docs/ pages don't exist yet (not because of config errors). Check that the error mentions missing files, not invalid config.</verify>
  <done>mkdocs.yml exists at project root with valid configuration, material theme, and mkdocstrings plugin</done>
</task>

<task type="auto">
  <name>Task 3: Create docs/index.md landing page</name>
  <files>docs/index.md</files>
  <action>Create docs/index.md as the documentation landing page with:

- H1: Sonny's Data Client
- Brief description (Python SDK for Sonny's Carwash Controls Data API)
- Feature highlights (8 resource types, auto-pagination, rate limiting, Pydantic models, batch jobs)
- Quick install snippet: `pip install git+https://github.com/.../sonnys-data-client.git`
- Quick start code example (create client, fetch transactions — keep it minimal, 5-6 lines)
- Links to API Reference sections (Client, Resources, Models, Exceptions)

Also create placeholder files so mkdocs build can succeed:
- docs/api/client.md (just a heading "# Client" for now)
- docs/api/resources.md (just a heading "# Resources" for now)
- docs/api/models.md (just a heading "# Models" for now)
- docs/api/exceptions.md (just a heading "# Exceptions" for now)

These placeholders will be replaced with real autodoc content in Plan 02.</action>
  <verify>Run `python -m mkdocs build --strict` succeeds with no errors or warnings. Run `python -m mkdocs serve` and confirm landing page renders at http://127.0.0.1:8000/ (stop server after confirming).</verify>
  <done>docs/index.md exists with landing page content, 4 placeholder API reference pages exist, mkdocs build --strict passes clean</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pip install -e ".[docs]"` succeeds
- [ ] `python -m mkdocs --version` shows mkdocs version
- [ ] `python -m mkdocs build --strict` passes with no errors
- [ ] mkdocs.yml has material theme and mkdocstrings plugin configured
- [ ] docs/index.md has landing page content
- [ ] docs/api/ has 4 placeholder pages (client, resources, models, exceptions)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- mkdocs builds successfully with material theme
- mkdocstrings plugin configured for src layout
- Documentation site renders locally
</success_criteria>

<output>
After completion, create `.planning/phases/12-api-reference-setup/12-01-SUMMARY.md`
</output>
