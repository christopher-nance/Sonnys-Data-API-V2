---
phase: 34-validation-deployment
plan: "01"
type: execute
---

<objective>
Validate labor CPC methods against real JOLIET car wash data and deploy final documentation to GitHub Pages.

Purpose: Confirm that total_labor_cost(), cost_per_car(), and report() produce correct, reasonable numbers against production data before closing the v1.3 milestone.
Output: Validated labor CPC feature with live docs deployed to GitHub Pages.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase summaries (direct dependencies):
@.planning/phases/31-report-integration/31-01-SUMMARY.md
@.planning/phases/32-stats-guide-update/32-01-SUMMARY.md
@.planning/phases/33-unit-tests/33-01-SUMMARY.md

# Key source files:
@src/sonnys_data_client/resources/_stats.py
@src/sonnys_data_client/types/_stats.py

**Tech stack available:** pytest, mkdocs-material, mkdocstrings, pydantic v2, requests
**Established patterns:** StatsResource with _fetch_all_clock_entries(), inline labor aggregation in report(), mkdocs gh-deploy --force for docs publishing
**Constraining decisions:**
- [Phase 31]: Labor cost computed inline in report() for shared data fetching efficiency
- [Phase 32]: Docs already deployed once with labor CPC content; this is the final deployment
- [Phase 33]: 148 unit tests passing including 11 labor-specific tests
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create live validation script for labor CPC methods</name>
  <files>scripts/validate_labor_cpc.py</files>
  <action>
Create a standalone Python script that validates the new labor CPC methods against real API data. The script should:

1. Accept API credentials via environment variables (SONNYS_API_ID, SONNYS_API_KEY, SONNYS_SITE_CODE) — fail with a clear message if missing
2. Use a recent 7-day date range (last full week) so there's enough data to validate but not so much that API calls are excessive
3. Exercise three methods in sequence:
   - `total_labor_cost(start, end)` — print total cost, regular/overtime breakdown, hours, and entry count
   - `cost_per_car(start, end)` — print cost per car, total labor cost, and total washes
   - `report(start, end)` — print full report including sales, washes, conversion, labor, and CPC
4. Format output as a clear, readable validation report with section headers and dollar formatting
5. Include a timing summary showing how long each method call took
6. Use `if __name__ == "__main__"` guard

Do NOT hardcode any credentials. Do NOT use argparse (keep it simple — env vars only). Do NOT add this script to package dependencies or imports.
  </action>
  <verify>python -c "import ast; ast.parse(open('scripts/validate_labor_cpc.py').read())" succeeds (syntax check)</verify>
  <done>Script exists at scripts/validate_labor_cpc.py, is syntactically valid, reads credentials from env vars, exercises all 3 labor CPC methods, and prints formatted output</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Validation script for testing labor CPC methods against real JOLIET data</what-built>
  <how-to-verify>
    1. Set environment variables with your JOLIET credentials:
       - SONNYS_API_ID
       - SONNYS_API_KEY
       - SONNYS_SITE_CODE (the JOLIET site code)
    2. Run: python scripts/validate_labor_cpc.py
    3. Verify total_labor_cost() output:
       - Total cost is a reasonable dollar amount (not zero, not astronomical)
       - Regular hours and overtime hours look realistic for 7 days of operations
       - Entry count corresponds roughly to number of employees x days
    4. Verify cost_per_car() output:
       - Cost per car is a reasonable amount (typically $2-$8 per car for a car wash)
       - Total washes is non-zero and realistic for 7 days
    5. Verify report() output:
       - All KPIs present (sales, washes, conversion, labor, cost_per_car)
       - Labor and cost_per_car numbers match the individual method calls above
       - Sales and wash counts look reasonable for the date range
  </how-to-verify>
  <resume-signal>Type "approved" to continue to docs deployment, or describe any issues with the numbers</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Deploy final documentation to GitHub Pages</name>
  <files>docs/index.md, mkdocs.yml</files>
  <action>
Run `python -m mkdocs gh-deploy --force` to deploy the final documentation to GitHub Pages. This ensures the live site has all v1.3 content (labor CPC method docs from Phase 32, updated API reference, and README).

Before deploying, verify the docs build succeeds locally with `python -m mkdocs build --strict` to catch any warnings or broken links.

Do NOT modify any documentation content — Phase 32 already updated all docs. This task is purely the deployment step.
  </action>
  <verify>python -m mkdocs build --strict succeeds with no warnings</verify>
  <done>Docs deployed to GitHub Pages via gh-deploy, mkdocs build --strict passes clean</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Validation script exists and is syntactically valid
- [ ] User confirmed labor CPC numbers are reasonable against JOLIET data
- [ ] `python -m mkdocs build --strict` passes with no warnings
- [ ] Docs deployed to GitHub Pages via `python -m mkdocs gh-deploy --force`
- [ ] All 148 existing tests still pass (`python -m pytest`)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Labor CPC methods validated against real production data
- Documentation live at GitHub Pages with full v1.3 content
- Phase 34 complete — v1.3 milestone ready for closure
</success_criteria>

<output>
After completion, create `.planning/phases/34-validation-deployment/34-01-SUMMARY.md`:

# Phase 34 Plan 01: Validation & Deployment Summary

**[Substantive one-liner about what was validated and deployed]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

Phase 34 complete — v1.3 Labor CPC milestone fully validated and deployed.
</output>
