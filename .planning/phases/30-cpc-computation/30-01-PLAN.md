---
phase: 30-cpc-computation
type: execute
---

<objective>
Implement `cost_per_car()` stats method that divides total labor cost by total washes.

Purpose: Provide the labor CPC metric — the primary KPI this milestone delivers.
Output: Working `client.stats.cost_per_car(start, end)` returning `CostPerCarResult`.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Direct dependencies:
@.planning/phases/29-labor-cost-computation/29-01-SUMMARY.md
@.planning/phases/24-conversion-rate/24-01-SUMMARY.md

# Key source files:
@src/sonnys_data_client/resources/_stats.py
@src/sonnys_data_client/types/_stats.py
@src/sonnys_data_client/__init__.py

**Tech stack available:** Pydantic v2, requests, datetime
**Established patterns:** Composed stat methods reusing existing methods (conversion_rate pattern), Google-style docstrings, zero-division safety
**Constraining decisions:**
- Phase 29: total_labor_cost() returns LaborCostResult with total_cost field
- Phase 22: total_washes() returns WashResult with total field (int)
- Phase 28: CostPerCarResult has 3 fields (cost_per_car, total_labor_cost, total_washes)
- Phase 29: CostPerCarResult already exported from top-level package
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement cost_per_car() on StatsResource</name>
  <files>src/sonnys_data_client/resources/_stats.py</files>
  <action>
Add CostPerCarResult to the import line from `sonnys_data_client.types._stats` (alongside existing ConversionResult, LaborCostResult, SalesResult, StatsReport, WashResult).

Add `cost_per_car(self, start, end) -> CostPerCarResult` method on StatsResource, placed AFTER `total_labor_cost()` and BEFORE `report()`. Follow the exact pattern of `conversion_rate()`:

1. Call `self.total_labor_cost(start, end)` to get LaborCostResult
2. Call `self.total_washes(start, end)` to get WashResult
3. Compute: `cpc = labor.total_cost / washes.total if washes.total > 0 else 0.0`
4. Return `CostPerCarResult(cost_per_car=cpc, total_labor_cost=labor.total_cost, total_washes=washes.total)`

Google-style docstring following conversion_rate() pattern — include Args, Returns, Raises, Example sections. Example should show:
```python
result = client.stats.cost_per_car("2026-01-01", "2026-01-31")
print(f"Cost per car: ${result.cost_per_car:.2f}")
print(f"Labor cost: ${result.total_labor_cost:.2f}")
print(f"Total washes: {result.total_washes}")
```

Do NOT modify the report() method — that happens in Phase 31.
  </action>
  <verify>python -c "from sonnys_data_client.resources._stats import StatsResource; print('import ok')" succeeds without errors</verify>
  <done>cost_per_car() method exists on StatsResource, imports resolve, method follows established composed stat method pattern</done>
</task>

<task type="auto">
  <name>Task 2: Verify package exports and run tests</name>
  <files>tests/test_types.py</files>
  <action>
1. Run `python -m pytest tests/ -x -q` to verify all tests pass (CostPerCarResult was already exported in Phase 29, so no code changes needed).

2. Verify end-to-end: `python -c "from sonnys_data_client import CostPerCarResult; print('exports ok')"` succeeds.

3. Verify the method has proper docstring: check that cost_per_car.__doc__ contains Args, Returns, Raises, Example sections.

No code changes expected in this task — purely verification that Phase 29 exports + Phase 30 method work together.
  </action>
  <verify>python -m pytest tests/ -x -q passes all tests. python -c "from sonnys_data_client import CostPerCarResult; print('exports ok')" succeeds.</verify>
  <done>CostPerCarResult importable from top-level package, all tests green, cost_per_car() has complete docstring</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `python -c "from sonnys_data_client import CostPerCarResult"` succeeds
- [ ] `python -c "from sonnys_data_client.resources._stats import StatsResource"` succeeds
- [ ] `python -m pytest tests/ -x -q` passes all tests
- [ ] cost_per_car() method has Google-style docstring with Args/Returns/Raises/Example
- [ ] cost_per_car() handles zero washes (returns 0.0, not ZeroDivisionError)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No import errors or test regressions
- cost_per_car() follows established composed stat method pattern (conversion_rate)
- CostPerCarResult importable from sonnys_data_client
</success_criteria>

<output>
After completion, create `.planning/phases/30-cpc-computation/30-01-SUMMARY.md`
</output>
