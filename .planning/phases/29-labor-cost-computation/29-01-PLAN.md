---
phase: 29-labor-cost-computation
type: execute
---

<objective>
Implement `total_labor_cost()` stats method that aggregates regular + overtime cost from clock entries.

Purpose: Provide the labor cost computation that Phase 30 (CPC) will divide by total washes.
Output: Working `client.stats.total_labor_cost(start, end)` returning `LaborCostResult`.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Direct dependencies:
@.planning/phases/27-labor-data-layer/27-02-SUMMARY.md
@.planning/phases/28-labor-cost-models/28-01-SUMMARY.md

# Key source files:
@src/sonnys_data_client/resources/_stats.py
@src/sonnys_data_client/types/_stats.py
@src/sonnys_data_client/types/_employees.py
@src/sonnys_data_client/__init__.py

**Tech stack available:** Pydantic v2, requests, datetime
**Established patterns:** Stat methods return dedicated result models, Google-style docstrings, _fetch_* helpers for data retrieval
**Constraining decisions:**
- Phase 27: _fetch_all_clock_entries() uses YYYY-MM-DD strings (not _resolve_dates), filters by site_code locally
- Phase 28: LaborCostResult has 7 fields (total_cost, regular_cost, overtime_cost, regular_hours, overtime_hours, total_hours, entry_count)
- ClockEntry fields used: regular_rate, regular_hours, overtime_rate, overtime_hours
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement total_labor_cost() on StatsResource</name>
  <files>src/sonnys_data_client/resources/_stats.py</files>
  <action>
Add LaborCostResult to the import line from `sonnys_data_client.types._stats` (alongside existing ConversionResult, SalesResult, StatsReport, WashResult).

Add `total_labor_cost(self, start, end) -> LaborCostResult` method on StatsResource, placed AFTER `conversion_rate()` and BEFORE `report()`. Follow the exact pattern of `total_sales()`:

1. Call `self._fetch_all_clock_entries(start, end)` to get all clock entries
2. Single-pass aggregation over entries:
   - `regular_cost += entry.regular_rate * entry.regular_hours`
   - `overtime_cost += entry.overtime_rate * entry.overtime_hours`
   - `regular_hours += entry.regular_hours`
   - `overtime_hours += entry.overtime_hours`
3. Derive: `total_cost = regular_cost + overtime_cost`, `total_hours = regular_hours + overtime_hours`, `entry_count = len(entries)`
4. Return `LaborCostResult(total_cost=..., regular_cost=..., overtime_cost=..., regular_hours=..., overtime_hours=..., total_hours=..., entry_count=...)`

Google-style docstring following total_sales() pattern — include Args, Returns, Raises, Example sections. Example should show:
```python
result = client.stats.total_labor_cost("2026-01-01", "2026-01-31")
print(f"Total: ${result.total_cost:.2f}")
print(f"Regular: ${result.regular_cost:.2f} ({result.regular_hours:.1f}h)")
print(f"Overtime: ${result.overtime_cost:.2f} ({result.overtime_hours:.1f}h)")
```

Do NOT modify the report() method — that happens in Phase 31.
  </action>
  <verify>python -c "from sonnys_data_client.resources._stats import StatsResource; print('import ok')" succeeds without errors</verify>
  <done>total_labor_cost() method exists on StatsResource, imports resolve, method follows established stat method pattern</done>
</task>

<task type="auto">
  <name>Task 2: Wire package exports and verify</name>
  <files>src/sonnys_data_client/__init__.py, tests/test_types.py</files>
  <action>
1. In `src/sonnys_data_client/__init__.py`:
   - Add `LaborCostResult` and `CostPerCarResult` to the import line from `sonnys_data_client.types` (alongside existing SalesResult, WashResult, ConversionResult, StatsReport)
   - Add `"LaborCostResult"` and `"CostPerCarResult"` to the `__all__` list

2. Run `python -m pytest tests/ -x -q` to verify all tests pass (the test_types.py __all__ count was already updated to 36 in Phase 28, so no change needed there).

Do NOT add LaborCostResult to `sonnys_data_client/types/__init__.py` — it was already added in Phase 28.
  </action>
  <verify>python -m pytest tests/ -x -q passes all tests. python -c "from sonnys_data_client import LaborCostResult, CostPerCarResult; print('exports ok')" succeeds.</verify>
  <done>LaborCostResult and CostPerCarResult importable from top-level package, all tests green</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `python -c "from sonnys_data_client import LaborCostResult"` succeeds
- [ ] `python -c "from sonnys_data_client.resources._stats import StatsResource"` succeeds
- [ ] `python -m pytest tests/ -x -q` passes all tests
- [ ] total_labor_cost() method has Google-style docstring with Args/Returns/Raises/Example
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No import errors or test regressions
- total_labor_cost() follows established stat method pattern (total_sales, conversion_rate)
- LaborCostResult and CostPerCarResult importable from sonnys_data_client
</success_criteria>

<output>
After completion, create `.planning/phases/29-labor-cost-computation/29-01-SUMMARY.md`
</output>
