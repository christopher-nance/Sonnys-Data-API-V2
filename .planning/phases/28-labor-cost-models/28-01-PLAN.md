---
phase: 28-labor-cost-models
type: execute
---

<objective>
Add Pydantic result models for labor cost and cost-per-car analytics.

Purpose: Define typed return objects for the `total_labor_cost()` (Phase 29) and `cost_per_car()` (Phase 30) stats methods, following the established SonnysModel pattern used by SalesResult, WashResult, and ConversionResult.
Output: LaborCostResult and CostPerCarResult models in _stats.py, exported from types/__init__.py.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-labor-data-layer/27-01-SUMMARY.md
@.planning/phases/27-labor-data-layer/27-02-SUMMARY.md

# Key files:
@src/sonnys_data_client/types/_stats.py
@src/sonnys_data_client/types/_base.py
@src/sonnys_data_client/types/__init__.py
@src/sonnys_data_client/types/_employees.py

**Tech stack available:** Pydantic v2 with SonnysModel base (camelCase aliases, populate_by_name)
**Established patterns:** Stats result models extend SonnysModel with typed fields + Google-style docstrings
**Constraining decisions:**
- [Phase 27]: ClockEntry has regular_rate, regular_hours, overtime_rate, overtime_hours fields
- [Phase 19]: Stats types live in types/_stats.py, extend SonnysModel
- [Phase 25]: StatsReport composes other result models (SalesResult, WashResult, ConversionResult)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LaborCostResult and CostPerCarResult to _stats.py</name>
  <files>src/sonnys_data_client/types/_stats.py</files>
  <action>
Add two new models to the end of _stats.py (before StatsReport), following the exact pattern of SalesResult/WashResult/ConversionResult:

**LaborCostResult** — returned by `client.stats.total_labor_cost()`:
- `total_cost: float` — regular_cost + overtime_cost
- `regular_cost: float` — sum of (regular_rate * regular_hours) across all clock entries
- `overtime_cost: float` — sum of (overtime_rate * overtime_hours) across all clock entries
- `regular_hours: float` — sum of regular_hours across all entries
- `overtime_hours: float` — sum of overtime_hours across all entries
- `total_hours: float` — regular_hours + overtime_hours
- `entry_count: int` — number of clock entries aggregated

**CostPerCarResult** — returned by `client.stats.cost_per_car()`:
- `cost_per_car: float` — total_labor_cost / total_washes (0.0 if zero washes)
- `total_labor_cost: float` — from LaborCostResult.total_cost
- `total_washes: int` — from WashResult.total

Each model needs a Google-style docstring matching the existing pattern (reference to the stats method, field explanations, zero-division note for CostPerCarResult).
  </action>
  <verify>python -c "from sonnys_data_client.types._stats import LaborCostResult, CostPerCarResult; print(LaborCostResult.model_fields.keys()); print(CostPerCarResult.model_fields.keys())"</verify>
  <done>Both models importable from _stats.py, all fields present with correct types</done>
</task>

<task type="auto">
  <name>Task 2: Export new models from types/__init__.py</name>
  <files>src/sonnys_data_client/types/__init__.py</files>
  <action>
Add LaborCostResult and CostPerCarResult to the import from _stats and to __all__:

1. Update the `from sonnys_data_client.types._stats import` line to include LaborCostResult and CostPerCarResult
2. Add both to the `__all__` list (alphabetical placement near other stats models)

Follow existing import style — models grouped by source module.
  </action>
  <verify>python -c "from sonnys_data_client.types import LaborCostResult, CostPerCarResult; print('OK')"</verify>
  <done>Both models importable from sonnys_data_client.types, listed in __all__</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `python -c "from sonnys_data_client.types import LaborCostResult, CostPerCarResult"` succeeds
- [ ] LaborCostResult has 7 fields: total_cost, regular_cost, overtime_cost, regular_hours, overtime_hours, total_hours, entry_count
- [ ] CostPerCarResult has 3 fields: cost_per_car, total_labor_cost, total_washes
- [ ] Existing tests pass: `python -m pytest tests/ -q`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No import errors or type issues introduced
- Models follow established SonnysModel pattern with Google-style docstrings
  </success_criteria>

<output>
After completion, create `.planning/phases/28-labor-cost-models/28-01-SUMMARY.md`
</output>
